AWS and Cloud Flare
User Name : aywaa@msn.com 
Password : Xw@r7v?gte


git clone https://gitlab.com/wanbuffer/tothiq-python-api.git .

git lab
wb.bharatparmar
Bharat@383#



pip install django
pip install rest_framework
pip install djangorestframework
python manage.py collectstatic
pip install --upgrade djangorestframework-simplejwt
pip install django-cors-headers
pip insall djongo
pip install psycopg2-binary


CREATE USER tothiquser WITH PASSWORD 'tothiqbetauser123';

CREATE DATABASE tothiq;
GRANT ALL PRIVILEGES ON DATABASE tothiq TO tothiquser;

sudo nano /etc/systemd/system/my_django_project.service

home/ubuntu/tothiq-beta/tothiq/

gunicorn tothiq.wsgi:application

sudo nano /home/ubuntu/tothiq-beta/tothiq.service


[Unit]
Description=Gunicorn instance for tothiq
After=network.target

[Service]
User=ubuntu
Group=ubuntu
WorkingDirectory=/home/ubuntu/tothiq-beta/tothiq/
ExecStart=/home/ubuntu/tothiq-beta/tothiq/venv/bin/gunicorn --workers 3 --bind unix:/home/ubuntu/tothiq-beta/totqhiq/tothiq.sock tothiq.wsgi:application

[Install]
WantedBy=multi-user.target

sudo systemctl start tothiq
sudo systemctl enable tothiq





$ sudo chown ubuntu tothiq.service
sudo chown ubuntu:ubuntu tothiq.service

sudo systemd-analyze verify tothiq.service


sudo nano /etc/nginx/sites-available/tothiq


============================


server {
    listen 80;
    server_name 65.0.55.246;

    location / {
        proxy_pass http://unix:/home/ubuntu/tothiq-beta/tothiq/tothiq.sock;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location /static/ {
        alias home/ubuntu/tothiq-beta/tothiq/static/;
    }

    location /media/ {
        alias home/ubuntu/tothiq-beta/tothiq/media/;
    }
}


===============================

sudo ln -s /etc/nginx/sites-available/tothiq /etc/nginx/sites-enabled

================================

sudo nginx -t
sudo systemctl restart nginx


==================

pip install Pillow
